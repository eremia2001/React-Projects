// Mapping Challenges : 

// CHALLENGE 1:

/* const nums = [1,2,3,4,5]

const squared = nums.map( function(number)
{
    return number * number
});

console.log(squared)
*/


// CHALLENGE 2:

/*
const names = ["alice","bob","charlie","daniel"]

const capitalNames = names.map((name) =>
{
    return name[0].toUpperCase() + name.slice(1)
})

console.log(capitalNames)
*/

// CHALLENGE 3:

const pokemon = ["Bulbasaur" ,"Charmander","Squirtle"]

const varPokemon = pokemon.map(pokemon =>  "<p>" + pokemon + "</p>"
 )

console.log(varPokemon)